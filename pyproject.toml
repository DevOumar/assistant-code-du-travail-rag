[project]
name = "assistant-code-du-travail-rag"
version = "0.1.0"
description = "Assistant RAG pour le Code du travail francais."
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"
